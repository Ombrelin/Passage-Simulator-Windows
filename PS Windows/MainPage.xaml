<Page
    x:Class="PS_Windows.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PS_Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox Header="Login" Text="{x:Bind login, Mode=TwoWay}" PlaceholderText="Login..."
                     HorizontalAlignment="Center" Width="400" />
            <PasswordBox Header="Password" Password="{x:Bind password, Mode=TwoWay}" PlaceholderText="Password..."
                         HorizontalAlignment="Center" Width="400" Margin="16" />
            <Button Click="handleClickLogin" HorizontalAlignment="Center" Content="Login" Width="400"/>
        </StackPanel>
    </Grid>
</Page>